# --- Core ---
Django>=5.0,<5.2
djangorestframework>=3.15

# --- Real-time layer (Section 3, 7) ---
channels[daphne]>=4.1
channels-redis>=4.2
# Cap below redis-py 6: channels-redis 4.3.0 declares only `redis>=4.6` and isn't
# compatible with redis-py 8.x's socket-timeout changes, which surface as
# "TimeoutError: Timeout reading from 127.0.0.1:6379" on idle websockets.
redis>=5.0,<6.0

# --- Database (Postgres in prod, Section 3) ---
psycopg[binary]>=3.2
dj-database-url>=2.2

# --- Config / env ---
django-environ>=0.11

# --- Static serving under daphne/ASGI (admin + DRF browsable API) ---
whitenoise>=6.6

# --- Frontend integration ---
django-cors-headers>=4.4

# --- Hyperliquid clients (Section 6/7) ---
websockets>=12.0
requests>=2.32

# --- Auth tokens for the React SPA ---
djangorestframework-simplejwt>=5.3

# --- v2 Trading Signals (Section 13, 20) ---
openai>=1.50
celery>=5.4

# --- v2 Auto-trade / broker execution (Section 13 follow-on) ---
# Fernet symmetric encryption for broker API keys at rest (never store raw).
cryptography>=42.0
# Bybit V5 unified-trading official SDK (scoped to Bybit for now).
pybit>=5.7
